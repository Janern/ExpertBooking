@model string
<form hx-get="Cart/Experts/@Model"
      hx-swap="outerHTML"
      hx-target="this"
      hx-trigger="CartChanged from:body">
    <button hx-delete="Cart/Experts"
            hx-swap="outerHTML"
            hx-target="this"
            type="submit" class="expertSelectedCheckmark">
        <img id="checkmark" class="expertSelectedCheckmark" src="images/svg/checkmark.svg" />
    </button>
    <input type="hidden" name="ExpertId" value="@Model"/>
</form>
